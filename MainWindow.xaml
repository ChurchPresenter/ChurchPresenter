<!-- ...existing code... -->
<DockPanel>
    <Menu DockPanel.Dock="Top">
        <MenuItem Header="_File">
            <MenuItem Header="New" />
            <MenuItem Header="Open..." />
            <MenuItem Header="Save" />
            <Separator />
            <MenuItem Header="Exit" />
        </MenuItem>
        <MenuItem Header="_Edit">
            <MenuItem Header="Undo" />
            <MenuItem Header="Redo" />
            <Separator />
            <MenuItem Header="Cut" />
            <MenuItem Header="Copy" />
            <MenuItem Header="Paste" />
        </MenuItem>
        <MenuItem Header="_View">
            <MenuItem Header="Zoom In" />
            <MenuItem Header="Zoom Out" />
            <Separator />
            <MenuItem Header="Full Screen" />
        </MenuItem>
        <MenuItem Header="_Help">
            <MenuItem Header="View Help" />
            <MenuItem Header="About" />
        </MenuItem>
    </Menu>
    <StackPanel Orientation="Horizontal" Margin="10">
        <!-- ...existing code... -->
        <Button Name="ChooseWindowsFontButton" Content="Choose Windows Font" Click="ChooseWindowsFontButton_Click" Margin="5"/>
        <Button Name="ChooseMacFontButton" Content="Choose Mac Font" Click="ChooseMacFontButton_Click" Margin="5"/>
        <!-- ...existing code... -->
    </StackPanel>
    <!-- ...existing code... -->
</DockPanel>
<!-- ...existing code... -->
